<?php

require 'functions.php';
require 'Database.php';
require 'router.php';

// create table notes / id / body (text) / not null
// create table users/ id / name (varchar 255)/ email (varchar 255) unique / user_id (int) not null / foreign_key
